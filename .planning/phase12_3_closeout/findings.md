# Findings

- The workspace is on `IELTS-WRITING-FEAT`, baseline `cc824c4`, with a large intentional uncommitted Phase 12 worktree. It must be treated as shared existing work, not discarded.
- Current known P0: `save_writing_draft` can overwrite a completed attempt as `Draft`; submission should only accept an open writing attempt.
- Current known P0: history score range compares Reading ratios and Writing bands as if they had one numeric scale. The query needs explicit scale semantics or the mixed view must not offer score range filtering.
- New migration `0008_history_retention_policy.sql` and Reading archive command/module are present but need independent audit plus merged tests.
- Settings/Library still contain suspected fake controls or browser-preference backup behavior; delete unsupported UI or replace it with a Rust command with truthful result handling.
- The only accepted visual authority is `apps/writing-vue/src/styles/opensource-skin.css`. Liquid Glass requires readable translucent layers, reduced-motion and high-contrast behavior; it must not be implemented by piling another override layer on top.
- Mandatory post-change gates, in order: `python developer/tests/ci/run_static_suite.py`, then `python developer/tests/e2e/suite_practice_flow.py`.
- Direct source scan confirms `HistoryFilter` currently applies raw `score_value` min/max predicates while the persisted rows retain `score_scale`; this is a concrete mixed-scale bug, not a UI-only concern.
- A combined `rg` lookup failed because PowerShell passed `history-repository.*` as an invalid literal path. Subsequent inspection must enumerate exact filenames instead of retrying that wildcard path.
- `ScoreScale` is declared in `crates/ielts-domain/src/domain.rs`, not in a guessed `domain/attempt.rs` module. The failed guessed read is logged; future domain reads use `domain.rs` directly.
- Completed Reading attempts persist `score_scale = ratio`; writing evaluation completion persists `band9`; legacy import maps the same way. A scale-aware SQL predicate will therefore be accurate for terminal rows.
- The current History UI shows 0–9 for all modes and sends raw numbers to one unified command. It can safely expose 0–100 only for Reading, convert to ratio in the adapter, and disable score filtering for the mixed view.
- `HistoryPage` duplicates list/export query construction and still includes a legacy fallback export path. This is a likely second truth boundary; keep it in scope after the typed query contract is fixed.
- The first broad HistoryPage patch failed because one later context anchor did not match the actual component layout. No hunk applied. Per the error protocol, subsequent UI changes are split into small, exact patches rather than retrying the broad patch.
- Writing state owner reports a completed fix: save/submit now use an immediate transaction and accept only open Writing attempts; focused Phase 5 and task-type tests pass. It remains uncommitted and needs merged verification.
- Archive audit found P0: repeated `annotation.id` values are silently `INSERT OR REPLACE`d during a committed import, and annotation asset references are not validated/stubbed. Both violate the promised validated atomic snapshot contract.
- Archive audit found P1: history retention clears `mode_idempotency` with a substring match on serialized JSON, so a short attempt ID can clear unrelated replay records; migration `0008` also truncates malformed numeric-looking legacy values (for example `150abc`) into a deletion policy.
- UI audit found P0: Reading backup list/create still persists complete archives into frontend preferences and reports success before an awaited durable write. It coexists with the native archive path and is a direct dual-truth violation. Settings cache/catalog controls and Reading refresh flags are also dead browser-era UI.
- Archive fix now validates annotation IDs across the entire document and against locally owned annotations before writing, and materializes every annotation asset stub inside the import transaction. The focused archive suite proves duplicate IDs, cross-attempt collisions, and detached asset backup integrity.
- Visual audit confirms the current skin relies on high-specificity override warfare rather than a true token source; this is queued after P0 data integrity. Any visual migration must remove competing declarations rather than add another CSS layer.
- Retention repair now strictly parses legacy `history_limit` in both migration and restore paths, and replaces raw substring idempotency deletion with JSON field-aware matching. Focused retention, backup, and mode tests pass; merged review remains required.
- The first visual-source cleanup moved the shared canvas, navigation active state, and base brand-button physical colors into `opensource-skin.css`, and added a dark Liquid Glass token branch. This is an incremental source-consolidation step, not a claim that all scoped legacy colors are gone.
- Reading Library cleanup removed all searched legacy archive/preference and fake-cache symbols. The only remaining archive success guard requires `committed === true && failed === 0`; data tools are now reachable as their own Library tab.
- Settings/History audit found a confirmed P1 History rendering regression at `HistoryPage.vue:285-287`: a score-styled field renders the submitted date rather than the record score. It is a narrow zero-risk correctness repair.
- Settings and History still carry extensive scoped legacy palette/theme blocks (including physical red/green/yellow/brown/gradient values and `--text-*`, `--bg-*`, `--bauhaus-*` aliases), which compete with `opensource-skin.css`. The next visual pass must delete or tokenise those blocks rather than add a new override sheet.
- Reading audit confirms the practice workflow is present, but `PracticeReadingPage.vue` still has a huge legacy scoped palette with physical values and `!important`; it must be treated as the next visual-source migration target, not covered over.
- Reading settings is a semantically incomplete modal (`v-show` without dialog semantics, Escape handling or focus management). Narrow visual/UX repairs are available: labels for icon-only controls, `role=status` for autonomous transitions/loading, and responsive single-column behavior below tablet width.
- EvaluatingPage is a product-language and visual outlier: English pseudo-pipeline wording, no unified token use, no accessible progress/live updates, and an empty-state path that can look like a permanent skeleton. These are evidence-backed P1 candidates for the next batch.
- Rust/Tauri audit finds no Vue invoke call missing from the registered command handler, so the main command graph is covered; this is evidence that native migration is substantial, not complete.
- Confirmed P1 native-boundary defects: `client.js` exports five always-throwing legacy APIs; `writing_submit_attempt` requires AI configuration before durable submission. The latter couples an offline product action to an optional evaluation provider and must be split before claiming a truthful native workflow.
- Confirmed P2 dual-truth remnants: Library exposes sessionStorage cache cleanup, Reading UI preferences retain localStorage migration logic, and compatibility shapes are scattered across adapters. Remove unused paths or concentrate compatibility at one adapter boundary.
- Reading CSS has duplicated layout definitions; its later responsive blocks override earlier ones. Any visual cleanup must consolidate the data structure first, rather than mechanically replacing colors across competing rules.
- Follow-up inspection found the frontend evaluation facade still performs the same AI preflight before calling Rust (`client.js:499-500`), so the command-level decoupling is not yet observable in the Compose flow. It must be removed or redesigned with a truthful post-submit state; merely changing Rust would be a half-fix.
- `evaluate.start` is also reused by EvaluatingPage retry even though it currently saves/submits first. Because submitted attempts are deliberately immutable, the adapter must distinguish initial persistence from retry/start-only semantics before a retry can be considered robust.
- The Library cache-clear UI really is the sole remaining production reference to old `sessionStorage` reading submission keys. Its removal requires updating stale test targets that still expect `clear-cache-btn`; those tests currently encode the obsolete product behavior and cannot be preserved as truth.
- Source audit after the checkpoint found a concrete remaining frontend-random selection in `PracticeLibraryPage.vue` (`Math.random` around `startRandomPractice`). It is product truth, not cosmetic randomness: selection must move to the existing Rust/Tauri session creation boundary.
- The only remaining production browser persistence scan hit is the documented one-shot `localStorage` migration in `useReadingUiPreferences.ts`; it currently copies/removes legacy keys into Tauri preferences. Removing it outright risks dropping legacy notes/preferences, so it remains a controlled P2 migration bridge rather than an active durable source.
- The visual child-component pass removed all identified visible Reading emoji without touching data/event contracts. The page-level pass removed its legacy variable aliases and competing token overrides, but source scan still shows roughly one hundred physical color declarations concentrated in its legacy CSS. That remaining block needs a data-structure-first cleanup, not a blanket search/replace.
- Packaged visual inspection after the latest gate confirms no horizontal overflow and a coherent blue-violet glass canvas, but exposes two real polish defects: Settings retains an emoji-like gear and oversized empty panels, while Reading Library renders duplicate overview labels (`总览 学习总览`) and cramped raw-symbol action labels. Reading practice layout itself is stable at desktop width.
- Session recovery on 2026-07-16 confirms the latest three screenshot-driven polish edits are still unvalidated. The shared diff is clean for whitespace, but generated E2E reports and Python bytecode must remain uncommitted. The next mandatory operation is static gate followed by packaged Tauri E2E; no commit can honestly precede those gates.
- Fresh packaged screenshots after the polish gate show the Reading practice workspace is structurally stable and History filters are legible. They also expose a real density/hierarchy problem: Library still nests a large `学习总览` glass panel inside the page canvas while leaving most of the viewport empty, and Settings still has four similarly weighted panels with raw diagnostic text instead of a task-oriented settings hierarchy. The Settings screenshot additionally captured malformed/clipped brand and top-nav text while the same header is correct on History/Library; this must be traced as a deterministic route/transition/layout defect before visual claims are made.
- `cargo fmt --check` and `git diff --check` pass after the latest changes. Direct source scan shows the claimed canonical skin is currently a mixed state: it defines the shared tokens but also contains local high-specificity and `!important` Reading overrides. That is still one file, but not yet a clean component/token data structure; a future visual pass must collapse component geometry and styling instead of expanding the override block.
- Native-gap audit found a concrete Settings failure: `apps/writing-vue/src/api/client.js` calls undefined `deleteKv` from `prompts.delete`, so deleting a prompt fails with `ReferenceError`. The durable Rust setting delete operation exists but is not exposed through the relevant command/adapter boundary.
- Direct contract scan confirms the prompt-delete failure at `client.js:391`; it also confirms a second rendered-data mismatch: `essays.getStatistics()` returns `{ total, averageScore, scored }`, while `HistoryPage` requires `{ count, latest, average }`. Its `hasWritingAnalytics` predicate is therefore permanently false and hides the writing analytics section. The repair needs one typed Rust-owned statistics DTO (including latest criterion scores and averages) rather than an ad hoc client-side history reduction.
- Flow audit found a concrete writing-state regression: Evaluating routes a cancelled/failed evaluation back to Compose with the same attempt id, but Rust correctly permits draft writes only on Draft/Active attempts. An interrupted or submitted attempt therefore fails the next auto-save/re-submit. The repair must clone the frozen content to a new open attempt or remove the false “continue editing” path; it must never reopen a submitted historical attempt.
- Current implementation batch decision: prompts are durable evaluation policy, so `settings(prompts)` plus a Vue-side deactivate loop is the wrong data structure. Move them to a dedicated SQLite aggregate with one active prompt per task enforced transactionally; add native history statistics; clone frozen writing input to a new draft for continuation.
- Native-gap audit also found a shipped-product claim mismatch: README still advertises ListeningPractice, while the Tauri bundle, Vue routes, and Rust commands contain no listening capability. This is a documented product-scope decision required before claiming full migration, not a hidden frontend feature.
- Completed native-gap audit establishes that the packaged app is substantially Tauri/Rust-native (no shipping HTTP/SSE/WebSocket business host; reading selection is Rust-owned), but it is not migration-complete. Confirmed P1 migrations still needed: Rust must own active-prompt uniqueness, writing-history statistics, atomic history deletion, Reading draft timer state, and failure-safe localStorage preference migration. `practice-client.js` and `evaluation-result.js` are debt concentrators, while frontend random IDs are lower-severity identity plumbing rather than product selection truth.
- Do not silently claim Listening migrated: README advertises it, but the shipped host has no route/command/resource support and the old `ListeningPractice/` is localStorage static legacy. It requires an explicit product decision before any “whole product complete” statement.
- The cancelled-writing repair design is now fixed: cloning must occur in Rust from the frozen writing draft into a newly generated `attempt-*` draft before navigation back to Compose. It preserves the original evaluation/history snapshot and avoids pretending a submitted/reviewing attempt can be reopened.
- While inspecting the shared worktree, `writing/draft.rs` acquired a Rust `clone_writing_draft` implementation with idempotency, terminal-source guard, UUID attempt allocation and transactional copy semantics. Treat it as concurrent work pending attribution/review; do not overwrite it. Integration still requires command registration, Evaluating navigation, and focused source/runtime regression coverage.
- Completed UI audit confirms a real visual architecture defect, not merely subjective polish: the canonical skin contains dark tokens but later hard-coded white surfaces, so dark mode can render light text over white panels. The same token file applies nested blur/elevation to workspace roots and inner cards, causing the glass-on-glass visual clutter observed in screenshots.
- Confirmed P0 usability defect: `ReadingHistoryPanel` advertises a focusable button but has no click/keyboard handler; its filter controls exist only on a CSS-flipped back face. Replace it with a normal visible control rather than retaining false ARIA/3D behavior.
- Confirmed P1 cleanup queue: `PracticeReadingPage` and `HistoryPage` have duplicate competing scoped CSS blocks; Compose imposes unnecessary viewport-height sidebars; multiple dialogs lack a unified focus/Escape behavior; semantic token aliases are incomplete; remaining emoji/physical colours still violate the unique source rule.
- Implemented the smallest truthful Reading trend repair: the front face now contains the actual range buttons and the card no longer claims button semantics it does not implement. Implemented an initial semantic surface pass in the sole skin file: raised surface, controls, inputs, nav, danger and info each use light/dark tokens; the global selectors no longer hard-code their light backgrounds.
- Concurrent implementation is broader than the initial clone service: it now also adds typed Rust writing-history statistics and atomic history deletion/clear APIs, replaces the client-side prompt KV facade with a prompt repository, and routes Evaluating cancellation through a cloned draft. These changes must be treated as one integrated batch and reviewed/tested together before any additional overlapping edits.
- Prompt aggregate owner reports its related checks pass, but the merged static suite is 17/18 because two v8 history-retention migration fixtures execute against a pre-v9 schema that lacks `history_retention_policy`. This is a compatibility defect in the migration test path, not a reason to waive the gate.
- Visual-source cleanup now also removes Settings' outer glass elevation (inner functional cards remain the sole surfaces), eliminates two remaining text emoji, and declares legacy animation/mono aliases from the canonical Liquid Glass tokens so existing scoped geometry does not silently lose transitions or typography.
- Repository size was build-cache debt, not shipping-asset debt: ignored `target/` held 27.27 GiB while all tracked product/test content remained small. Safe cleanup must target Cargo/dist/report/bytecode outputs only; the Reading resource pack and migration fixtures are product truth and must remain.
- Fresh packaged screenshots after the prompt/history/clone batch show no overflow or route collapse. Remaining visible debt is concrete: Compose mixes an English marketing heading with Chinese controls and wastes vertical space; Settings nests a raw white diagnostics sheet inside glass; History's empty state is an oversized blank surface; Reading shows a persistent recovery strip above the workspace. These are the next narrow UI repairs, not grounds for another global override sheet.
- Preference migration had a deterministic data-loss order: `takeLocal` deleted source bytes before asynchronous SQLite/annotation persistence. The repair uses strict hydration and awaitable durable writes, then removes the legacy key. Active suite preferences in PracticeLibrary already use SQLite despite misleading helper names and are not a second browser-storage truth.
- Timer audit separates proven defects from a false lead: malformed Suite JSON did silently reset and is now rejected; Suite merge ignores the frontend's stale `durationSeconds`, so that extra field cannot corrupt its anchor/paused state. Single and Endless practice still lack a durable timer table/session field and require a versioned schema plus backup-table inclusion.
- Fresh post-fix screenshots confirm the narrow UI changes work. Compose still has intentionally large writing workspace geometry, but no longer mixes English marketing headings; Settings technical paths are collapsed; History empty state is compact; Reading recovery notice is centered and bounded.
